name: Background Tasks

on:
  push:
    branches:
      - main
    paths:
      - server.bat

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Start server in background
        run: |
          cmd /c "START /b server.bat"
          # Might need to sleep for a bit here for the server to bootstrap
          curl -X GET "http://localhost:9195/files/plain.txt"
